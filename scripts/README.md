#!/usr/bin/env python3
"""
Setup and development guide for the video-subtitle translation pipeline.

Provides instructions for creating a Python virtual environment, installing
dependencies, and setting up optional Automatic Speech Recognition (ASR)
support for subtitle-less videos.

Usage:
    # Create virtual environment
    python3 -m venv .venv

    # Activate virtual environment
    source .venv/bin/activate

    # Install required dependencies
    pip install -r requirements.txt

    # Install optional ASR dependencies (for subtitle-less videos)
    pip install -r requirements-asr.txt
"""
